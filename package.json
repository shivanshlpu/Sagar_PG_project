{
  "name": "pg-management-platform",
  "version": "1.0.0",
  "private": true,
  "description": "PG Management & Tenant Communication Platform",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:web": "cd web && npm run dev",
    "dev:queue": "cd services/queue && npm run dev",
    "dev:whatsapp": "cd services/whatsapp && npm run dev",
    "build:server": "cd server && npm run build",
    "build:web": "cd web && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../web && npm install && cd ../services/queue && npm install && cd ../services/whatsapp && npm install && cd ../services/pdf && npm install"
  }
}
